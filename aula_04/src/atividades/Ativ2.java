package atividades;

import java.util.Scanner;

public class Ativ2 {

	public static void main(String[] args) {

	/*	Scanner leia = new Scanner(System.in);

		int numero, vetor[] = new int[10], localizador = 0, pares = 0, indice = 0, impares = 0, soma, media;

		
		for (int entrada = 0; entrada < vetor.length; entrada++) {
			System.out.println("Digite uma valor para a posica [" + entrada + "]");
			vetor[entrada] = leia.nextInt();
		}

		
		for (indice = 0; indice < vetor.length; indice++) {
			if (indice % 2 != 0) {
				impares++;
				int indicesImpares[] = new int[impares];
				for(int i = 0; i < indicesImpares.length; i++ )
					indicesImpares[i] = vetor[indice]; 
			}
			
			if (vetor[indice] % 2 == 0) {
				pares++;
			}
		}

		int elementosPares[] = new int[pares];
		
		
		
		System.out.println("Elementos nos índices ímpares: ");
		for(var numeroIndice: indicesImpares) {
			System.out.println(numeroIndice);
		}
		
		System.out.println("Elementos pares: ");
		if (indice % 2 != 0) {
			pares++;
		}

		leia.close();  */
	}

}
